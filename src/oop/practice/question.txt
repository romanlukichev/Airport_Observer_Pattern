Надо ли писать функцию Display как в базовом классе, так и в интерфейсе?
В базовом классе InformingDevice написал, чтоб из Flight (Subject) можно было вызвать метод по списку в цикле For Each
А в интерфейсе написал просто потому что так требуется соглано описания шаблона Observer.

В кажом классе Display Mobile Widget SMS  (Observers) пришлось переопределить эту функцию как
из базового класса InformingDevice так и из интерфейса Observer.